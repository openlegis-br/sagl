<dtml-comment>
connection_id : dbcon_openlegis
max_rows : 1
max_cache : 100
</dtml-comment>
SELECT 
DISTINCT REPLACE(LAST_INSERT_ID(MAX(cod_substitutivo)), 'L', '') as cod_substitutivo
FROM 
substitutivo
