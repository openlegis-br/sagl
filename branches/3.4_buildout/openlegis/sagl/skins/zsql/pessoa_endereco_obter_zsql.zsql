<dtml-comment>
connection_id : dbcon_interlegis
arguments :
max_rows : 20000
max_cache : 100
</dtml-comment>
SELECT DISTINCT
       end_residencial

FROM   
       pessoa

WHERE 
       end_residencial IS NOT NULL
       and ind_excluido = 0 

ORDER BY
       end_residencial

