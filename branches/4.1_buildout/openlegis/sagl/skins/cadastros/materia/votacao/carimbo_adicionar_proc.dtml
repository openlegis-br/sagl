<dtml-call expr="REQUEST.set('hdn_url', 'materia_mostrar_proc?cod_materia=' + str(cod_materia) + '#votacao')">
<dtml-call expr="modelo_proposicao.requerimento_aprovar(cod_sessao_plen=hdn_cod_sessao_plen, nom_resultado=nom_resultado, cod_materia=cod_materia)">
<dtml-let mensagem="'Carimbo adicionado com sucesso!'" url="hdn_url" >
  <dtml-var mensagem_emitir>
</dtml-let>

