<dtml-comment>
connection_id : dbcon_interlegis
max_rows : 1
max_cache : 100
</dtml-comment>
SELECT 
  distinct REPLACE(LAST_INSERT_ID(), 'L', '') as cod_documento_acessorio
FROM 
  documento_acessorio_administrativo
